struct Point {
    x: i32,
    y: i32,
}

impl Point {
    inline fn new(x: i32, y: i32): Point {
        Point { x: x, y: y }
    }

    inline fn sum(&self): i32 {
        self.x + self.y
    }
}

fn main(): i32 {
    let p = Point::new(10, 20);
    p.sum()
}
