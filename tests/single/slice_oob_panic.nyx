// slices: a dynamic out-of-bounds index aborts at runtime with exit 101

fn main(): i32 {
    let a: [i32; 3] = [1, 2, 3];
    let s: &[i32] = &a;

    let mut i: uptr = 0;
    while i < 3 {
        i = i + 1;
    }

    s[i]
}
