use std::io::{println, print};
use std::process::{exit};

fn main() {
    println("hello, world!");

    let max = i8::MAX;
    let max_abs = max.abs();

    let name = "John Doe";
    let age = 42;

    print("{name} is {age} years old!");

    exit(0);
}

