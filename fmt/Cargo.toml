[package]
name = "fmt"
version = "0.1.0"
edition = "2024"


[dependencies]
frontend = { path = "../frontend" }
serde = { version = "1", features = ["derive"] }
toml = "1.1"

[dev-dependencies]
indoc = "2"
