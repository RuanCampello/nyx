[package]
name = "frontend"
version = "0.6.0"
edition = "2024"

[lib]
bench = false

[dependencies]
ariadne = "0.6.0"
bumpalo = "3"
lasso = { version = "0.7.3", features = ["multi-threaded"] }
macros = { path = "../macros" }

[dev-dependencies]
rstest = { workspace = true }
